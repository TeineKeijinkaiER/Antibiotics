# 抗菌薬投与ナビ（仮称）

手稲渓仁会医療センター『抗菌薬投与マニュアル 第9版』をもとにした、院内向け抗菌薬投与量参照アプリ。

> **「薬剤名 or 菌名を打てば、その患者に投与できる量が3タップ以内で出る」**

## 現在のステータス

**Phase 0〜3 実装済み・院内レビュー待ち。** 原典のデータ構造化とアプリ本体が動作し、完全オフラインで利用できます。

| | |
| --- | --- |
| 収載データ | 薬剤 74件／菌 40件／疾患 83件／適応外使用 68件／アンチバイオグラム 80行／周術期 15件／曝露後予防 6疾患 |
| 品質ゲート | `npm run validate` — 全レコードの原典ページ必須、参照切れ・重複・値域を検査 |
| ビルド | `npm run build`（validate → typecheck → vite build の順に実行） |

## 動かす

```bash
npm install
npm run dev        # 開発サーバ
npm run validate   # データ品質ゲートのみ実行
npm run build      # 検証 + 型チェック + 本番ビルド
```

## PWAとして使う（オフライン・ホーム画面への追加）

`npm run build` で `dist/` に Service Worker（`sw.js`）と `manifest.webmanifest` が生成され、
ビルド済みアプリはインストール可能な PWA として配信できます。

- **オフライン動作**: 初回アクセス時に全アセットをキャッシュし、以降はネットワークなしで起動・動作します
  （画面遷移はキャッシュ済み `index.html` を返し、アセットは cache-first）
- **ホーム画面への追加**: iOS Safari は「共有 → ホーム画面に追加」、Android Chrome は
  自動表示されるインストールバナーまたはメニューの「アプリをインストール」から追加できます
- **更新の反映**: 新しいビルドを配信すると、次回アクセス時に画面下部へ更新通知が出るので
  ボタン一つで最新版に切り替わります（`src/lib/sw.ts` の `applyUpdate`）
- サブパス配信（例: `https://<user>.github.io/Antibiotics/`）でも `vite.config.ts` の
  `base: "./"` と `manifest.webmanifest` / `sw.js` の相対パス解決により問題なく動作します

### GitHub Pages への公開

`.github/workflows/deploy.yml` により、対象ブランチへの push で自動的に
`npm run build` → GitHub Pages へのデプロイが実行されます（GitHub Actions の
Pages デプロイ機能を使用。`gh-pages` ブランチは使いません）。

初回のみ、リポジトリの **Settings → Pages → Build and deployment → Source** を
「GitHub Actions」に切り替える必要があります（リポジトリ管理者の操作）。

公開URLはプロジェクトページ形式になります: `https://<組織/ユーザー名>.github.io/Antibiotics/`

> ワークフローのトリガーは現在のデフォルトブランチ（`claude/antibiotic-app-planning-b4e13g`）に
> 設定しています。今後 `main` 等の恒久的なデフォルトブランチへ移行した場合は、
> `.github/workflows/deploy.yml` の `on.push.branches` を書き換えてください。

GitHub Pages（一般公開）での配信は 2026-08-27 に決定済みです（要件 NFR-007）。患者条件（体重・Cr等）は
端末内でのみ計算し外部送信しないため（NFR-006）、公開範囲を広げても患者情報の漏えいリスクは生じません。
適応外使用・採用薬・アンチバイオグラム等の院内固有情報は他施設に一般化できないため、初回起動時の確認画面・
全画面フッタ・「アプリの説明」で適用範囲を明示しています（FR-012）。

## ドキュメント

| ファイル | 内容 |
| --- | --- |
| [docs/01_企画書.md](docs/01_企画書.md) | 背景・課題、コンセプト、想定ユーザー、フェーズ分割、技術方針、リスク |
| [docs/02_要件定義書.md](docs/02_要件定義書.md) | 機能要件（FR-000〜FR-010）、非機能要件、画面一覧、スコープ外、未決事項 |
| [docs/03_データモデル.md](docs/03_データモデル.md) | エンティティ設計、JSON構造、データ整備手順、品質ゲート |
| [docs/04_アンチバイオグラム突合手順.md](docs/04_アンチバイオグラム突合手順.md) | 原データとの突合の3段階（自己整合チェック／機械突合／目視照合）と完了の記録方法 |
| [docs/07_医学的レビュー_原典修正点.md](docs/07_医学的レビュー_原典修正点.md) | 原典PDFの医学的修正点、一次資料、採用・実装状況 |
| [docs/08_引き継ぎ_医学的修正_UI改善.md](docs/08_引き継ぎ_医学的修正_UI改善.md) | Claude Code向けの変更内容、設計判断、検証方法、残課題 |

## 実装済みの機能

| ご要望 | 要件 | 実装 |
| --- | --- | --- |
| 抗菌薬名で検索し通常量がすぐ示される | FR-002 / FR-003 | 一般名（英・カナ）／商品名／略語／系統を横断検索。ひらがな・全角半角・長音の有無を吸収し、検索結果カードに通常量を直接表示 |
| 腎機能低下時の投与量がすぐわかる | FR-001 / FR-004 | Cockcroft-Gault で CCr を自動計算し、該当区分をハイライト・他区分を淡色化。HD/CHDF は CCr 区分より優先。記載のない区分は補完せず「原典に記載なし」と表示 |
| 成人と小児を分ける | FR-000 | 内服薬・注射薬を選んだ直後に必須選択（既定値なし）。選択中の集団の用量しか表示せず、小児では成人向けの腎機能表を出さない。小児は体重未入力なら mg/kg 表記のみ（Phase 3 で選択の位置を変更）|
| バンコマイシン等の投与設計 | FR-005 | VCM（AUC/MIC 400-600を基本目標、eGFR区分、1日4g上限警告）、TEIC、GM/AMK、VRCZ（CCr<30では原則静注を避ける旨を警告） |
| 適応外使用を薬剤名・疾患名の双方から検索 | FR-006 | 「薬剤 × 疾患 × 用法用量 × 区分 × 対象集団」の関係データ。成人・小児別に絞り込み、初期状態は折り畳み |
| 菌名検索とアンチバイオグラム | FR-007 | 学名／日本語名／略号等で検索。入院・外来タブ、感性率、検出株数、30株未満の注意喚起。抗菌薬略号から薬剤詳細へ遷移 |

### Phase 1 で追加した画面

| 画面 | 要件 | 内容 |
| --- | --- | --- |
| 周術期予防抗菌薬 | FR-008 | 領域別の推奨薬、体重帯から1回量を自動選択、β-ラクタムアレルギー時の代替、投与タイミング・術中追加投与・投与期間、手術創分類 |
| ペニシリンG持続静注 | FR-005-5 | 1日総量×投与経路から調製手順・K濃度。ローディングを先頭に固定表示し、1200万単位未満では間欠投与を推奨する警告 |
| 曝露後予防投与 | FR-009-3 | B型肝炎・HIV・水痘・インフルエンザ・髄膜炎菌・H. influenzae |
| 当院採用注射抗菌薬一覧 | FR-009-1 | 系統別に規格・薬価・投与可否・投与時間・申請書要否・配合変化。薬剤マスタから導出しデータを重複させない |
| 小児の体重・薬用量 | FR-009-4 | 年齢別体重平均値、Augsberger式・Young式、Von Harnack表 |
| 適正使用指針・AWaRe | FR-009-5 | 適正使用指針と申請ルール、内服薬のAWaRe分類 |

### Phase 2 で追加した機能

| 機能 | 要件 | 内容 |
| --- | --- | --- |
| PWA・完全オフライン動作 | NFR-002 | 全アセットを Service Worker で precache。通信は初回インストール時のみ。ホーム画面に追加してアプリとして起動できる |
| 更新の検知 | NFR-010 | 新しいデータ版を検知するとアプリ内にバナーを出し、その場で適用できる |
| お気に入り・閲覧履歴 | FR-010-1/2 | よく使う薬剤・菌をホームにピン留め、直近10件の履歴。保存するのはIDのみで患者条件は保存しない |
| AMR対策 | FR-009-5 | 抗微生物薬適正使用の手引き、AMRアクションプラン成果指標、参考文献 |

オフライン動作は実ブラウザでネットワークを切断して検証しています（起動・検索・薬剤詳細・
お気に入り・各リファレンスページまでエラーなく動作）。

### Phase 3 で行ったUI再編

原典の章立てに沿った導線へ作り替えました。データ・臨床コンテンツには変更を加えていません。
経緯と決定事項は [docs/06_UI再編計画.md](docs/06_UI再編計画.md)。

> その後、2026-08-28の医学的レビューに基づき臨床データも修正しています。現行仕様は
> [医学的レビュー](docs/07_医学的レビュー_原典修正点.md) と
> [引き継ぎ書](docs/08_引き継ぎ_医学的修正_UI改善.md) を参照してください。

| 変更 | 内容 |
| --- | --- |
| オープニング画面 | タイトル「TKH抗菌薬投与マニュアル 9th edition」、イラスト、大項目5ボタン（**内服薬／注射薬／感染症別／菌種別／その他**）のみ。説明文は置かない。「アプリの説明」は上部の欄外に小さく配置 |
| 「その他」の内容 | 周術期／暴露後予防投与／小児体重服用量簡易表／AMR対策 の4つ |
| 成人／小児 | 内服薬・注射薬・感染症別を選んだ直後に毎回選ぶ（2ボタンのみの画面）。菌種別では選ばせない |
| 感染症別 | 厚生労働省『抗微生物薬適正使用の手引き 第四版』から一部採用。急性下痢症（成人／小児別）と院内発症感染症を収載 |
| 内服薬の選択 | 薬剤名の入力欄 → AWaRe分類の解説 → **Access / Watch / Reserve の大ボタン**。理由がなければ Access から選ぶことを強調。分類を選ぶと次画面で薬剤名を選択。AWaRe対象外の薬剤は従属的な「その他」ボタンから |
| 注射薬の選択 | 薬剤名の入力欄 → 系統の大ボタン → 薬剤名の選択 |
| 薬剤ごとのツール | 投与設計（TDM）・ペニシリンG持続静注・当院採用注射抗菌薬一覧は大項目に並べず、**それぞれの薬剤の説明ページから開く** |
| 小児での表示 | 原典の腎機能低下時・透析・CHDF の投与量表は成人向けのため、**小児では表示しない**（成人対象である旨と相談先のみを示す） |
| アナフィラキシー | 項目を削除 |
| TDM画面の是正 | 患者条件が未入力だと初回投与量が表示されない（VCM）、逆に腎機能正常時の用量が既定値として出る（アミノグリコシド）等、薬剤ごとに異なっていた欠測時の挙動を統一。初回投与量と採血タイミングを常に提示し、採血日時を算出できるようにした |
| 表組みの是正 | 数値表と文章表でCSSを分離。狭い画面では文章表をカード表示に落とし、横スクロールを不要にした |
| 適用範囲の明示 | **当院の院内利用を想定したアプリ**である旨を、初回起動時の確認画面・アプリの説明画面・全画面のフッタに表示 |
| アプリの説明画面 | 使い方、注意事項、アップデートとオフライン利用、患者情報の扱い、免責事項の全文、原典・問い合わせ先 |
| 戻る／ホーム | すべての画面に配置。「戻る」は直前の画面へ1つ戻り、「ホーム」はオープニングへ直接戻る |
| 患者条件フォームの出し分け | 用量が患者条件で変わる画面（薬剤レーンと周術期）にのみ表示。菌種別・暴露後予防投与・小児体重簡易表・AMR対策では出さない |
| 小児の患者条件 | **体重のみ**を入力させ、mg/kg の用量を1日投与量（mg）に換算して強調表示。年齢・性別・身長・Cr・eGFR・腎代替療法の欄は出さない（いずれも成人向けの計算にしか使わないため） |
| 周術期の1回投与量 | 体重帯とその隣に用量を並べる配置に変更。用量の頭が縦に揃い、該当体重帯を強調 |
| 用量上限の単位の是正 | 原典の「400mg/kg/dayまで」等の**体重あたりの上限**が絶対量として保存されており、小児で5〜10倍の過少投与量を表示していた不具合を修正（下記） |

オープニングのイラストは `public/opening.jpg` があればそれを使い、無ければ同梱の
`public/opening.svg` にフォールバックします。写真を差し替えるときは `public/opening.jpg` を置いてください。

### 品質ゲート

```bash
npm run build             # データ検証 → 型チェック → ビルド → SW生成
npm run check:layout      # 幅375/768/1280px × 20画面の配置検査（要 vite preview）
npm run check:navigation  # 画面構成の回帰テスト（要 vite preview）
npm run check:tdm         # TDMの安全要件の回帰テスト（要 vite preview）
npm run check:disclaimer  # 適用範囲の明示とアプリの説明の回帰テスト（要 vite preview）
npm run check:medical-ui   # 適応外表示・用量強調・菌種別からの遷移（要 vite preview）
npm run check:sw           # 更新時の白画面防止・旧ハッシュ回復処理（build後）
```

`check:tdm` は「患者条件が未入力のときに用量の既定値を出さない」という安全要件を含みます。
表示の崩れではなく**誤った用量を出さない**ための検査であり、再発しうるため自動化しています。
`check:navigation` は小児で成人向けの情報が出ないことを、
`check:disclaimer` は院内利用を想定した旨の宣言が消えていないことを確認します。

## 構成

```
src/
  types.ts                        データ型（source は全レコード必須）
  data/                           医薬品データ（コードから完全分離）
    drugs.betalactam.json         β-ラクタム系 18件
    drugs.antibacterial.json      非β-ラクタム系・経口・抗結核 35件
    drugs.antifungal-antiviral.json  抗真菌・抗ウイルス・駆虫 21件
    organisms.json                菌マスタ（複数表記の別名を保持）
    antibiogram.json              アンチバイオグラム（入院／外来）
    offlabel.json / diseases.json 適応外使用と疾患マスタ
    prophylaxis.json              周術期予防抗菌薬
    reference.json                曝露後予防・小児体重・PCG持続静注・指針・AWaRe・AMR対策
  lib/
    normalize.ts                  日本語の表記ゆれ正規化
    search.ts                     全文検索と適応外の双方向インデックス
    calc.ts                       CCr・IBW・AdjBW・mg/kg 換算・腎機能区分の解決
    storage.ts                    お気に入り・閲覧履歴（localStorage）
    sw.ts                         Service Worker の登録と更新検知
  components/                     画面
public/                           manifest・PWAアイコン（scripts/make-icons.mjs で生成）
scripts/
  validate-data.mjs               データ品質ゲート
  make-sw.mjs                     Service Worker 生成（ビルド後に実行）
  make-icons.mjs                  PWAアイコン生成（zlibのみ、依存なし）
  check-antibiogram-consistency.mjs / make-checksheet.mjs / apply-corrections.mjs / diff-antibiogram.mjs
```

### 修正した用量上限の不具合（2026-08-27）

原典の「〜まで」には**絶対量**（1回600mgまで）と**体重あたり**（400mg/kg/dayまで）の2種類があり、
後者が絶対量のキー（`maxPerDay`）に格納されていたため、換算値が桁違いに小さくクリップされていました。

| 薬剤 | 原典 | 体重20kgでの表示（修正前 → 修正後） |
| --- | --- | --- |
| アンピシリン | 100-200mg/kg/day（400mg/kg/dayまで） | 400mg → **2000-4000mg** |
| アモキシシリン | 1日20-40mg/kg（90mg/kg/dayまで） | 90mg → **400-800mg** |
| イミペネム・シラスタチン | 30-60mg/kg/day（100mg/kg/dayまで） | 100mg → **600-1200mg** |

体重あたりの上限を `maxPerKgPerDose` / `maxPerKgPerDay` へ分離し、換算前の mg/kg に適用するよう変更しました。
`validate-data.mjs` に「絶対量の上限が mg/kg の値以下」を検出する検査を追加し、再発を防いでいます。

### 「1回◯mgまで」の自動計算（2026-08-27 実装）

1日量に対する「1回◯mgまで」（リネゾリド・ピペラシリン・シプロフロキサシン・アシクロビル等、
19レコード）は、原典の「分◯」の記載から分割回数を `divisionsPerDay` として構造化し、
1日量をその回数で割った1回あたりの値に上限を適用するようにしました。

| 薬剤 | 原典 | 体重70kgでの表示 |
| --- | --- | --- |
| リネゾリド | 30mg/kg/day 分3（1回600mgまで） | 1回 **600mg**（上限）を1日3回／1日総量 **1800mg**（クリップ後の値に整合） |

1回量が上限でクリップされる場合、見出しの1日総量も「クリップ後の1回量 × 分割回数」に
揃えて表示します（実際には投与できない見かけ上の1日量を出さないため）。
分割回数が原典の文章から読み取れない用量は、従来どおり自動適用せず注記として表示するに留めています。
`validate-data.mjs` に、`text` の「分◯」表記と `divisionsPerDay` の見落とし・不一致を検出する検査を追加しました。

## 既知の要検証事項

- イミペネム・シラスタチンの原典p.16 `1g 6時間毎` は添付文書上限を超えるため、アプリでは不採用とし、
  現行添付文書に基づく最大2g/日と薬剤固有のCCr区分へ修正済み。原典PDF自体は未修正のため次版原稿での差し替えが必要。
- アンチバイオグラムで感性率が菌数分の整数にならないセルが4つある（入院GGSのCLDM、外来B. fragilisのCLDM、
  外来E. faeciumのPCG・ABPC）。その抗菌薬の被検株数が菌数と異なることを示唆するもので、原本とは一致している。
  分母の扱いは検査部への確認事項として残っている。
- プログラム医療機器（SaMD）該当性の確認、原典PDF同梱の可否、菌の日本語名リストの監修は未決（docs/02 §5 参照）。

## アンチバイオグラムの原本照合

**2026-08-26 に入院・外来 全80行の目視照合を完了**（`antibiogram.json` の `meta.verified: true`）。
グラム陽性球菌で列ずれが見つかり、レンサ球菌5行の ABPC → CTRX、肺炎球菌2行の ABPC → MEPM を
入院・外来それぞれに適用した（計28セル）。グラム陰性桿菌は訂正なし。
手順と実施記録は [docs/04_アンチバイオグラム突合手順.md](docs/04_アンチバイオグラム突合手順.md) を参照。

```bash
npm run antibiogram:check                        # 自己整合チェック（加重平均で列ずれを検出）
npm run antibiogram:sheet > checksheet.html      # 原典と同じ列順・行順の照合シート → A4横で印刷
npm run antibiogram:corrections corrections.txt  # 記入した訂正を確認（--apply で反映）
```

年次改訂時は `meta.verified` を `false` に戻し、同じ手順を通す。

## 免責

本アプリが示す投与量は当院でコンセンサスの得られた標準的な投与量であり、最終的な投与判断は主治医が行います。
使用時は添付文書を改めて精読し、必要に応じて感染症科・ICT／AST へコンサルテーションしてください。
本アプリは院内マニュアルの閲覧を支援するものであり、診断・治療方針の提示は行いません。
